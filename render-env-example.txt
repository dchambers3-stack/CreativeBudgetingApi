# Render Environment Variables Configuration
# Copy these to your Render service's Environment tab

# Database (automatically set by Render PostgreSQL service)
# DATABASE_URL=postgresql://username:password@host:port/database

# Application Environment
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:10000

# Email Configuration
EMAIL_FROM_EMAIL=dchambers@creativecanvasdesigns.net
EMAIL_SMTP_HOST=smtp.titan.email
EMAIL_SMTP_PORT=587
# EMAIL_PASSWORD=your-email-password (set this manually in Render dashboard)

# CORS Origins (Update with your actual Netlify URL)
# ALLOWED_ORIGINS=https://your-netlify-app.netlify.app